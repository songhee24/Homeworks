Как вы понимаете hashCode?
это метод hashCode() позволяющий получить уникальный целый номер для данного объекта

Для чего мы можем переписать метод генерирования hashCode?
при создании  класса принято переопределять методы hashCode() и equals() таким образом что бы учитывались поля объекта

Как работает HashSet?
Это такая коллекция элементы которой неупорядоченны и уникальны
они присутствуют в коллекции не более одного раза

Какие отличия есть у HashSet & ArrayList?
это разные коллекции если положить один и тот же объект два раза в ArrayList и в HashSet то HashSet не добавить схожий объект повторно

Как вы думаете, как мы можем избавиться от дубликатов в ArrayList при помощи HashSet?
Set<String> set = new HashSet<>(yourList);
yourList.clear();
yourList.addAll(set);